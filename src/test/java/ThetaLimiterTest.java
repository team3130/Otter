import edu.wpi.first.math.geometry.Translation2d;
import frc.robot.Constants;
import frc.robot.commands.Chassis.ThetaLimiter;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class ThetaLimiterTest {/*
    @Test
    void simpleTest() throws InterruptedException {
        ThetaLimiter thetaLimiter = new ThetaLimiter(Constants.Swerve.kMaxThetaChange, .1, new Translation2d());
        var joystick = new Translation2d(1,0);
        try {
        Thread.sleep(20);}
        catch(Exception e){}
        Translation2d ghostTurn = thetaLimiter.calculate(joystick);
    }
    @Test
    void thetaTest() {
        ThetaLimiter thetaLimiter = new ThetaLimiter(Constants.Swerve.kMaxThetaChange, .1, new Translation2d(1,0));
        var joystick = new Translation2d(0,1);
        try {
            Thread.sleep(20);}
        catch(Exception e){}
        Translation2d ghostTurn = thetaLimiter.calculate(joystick);
        */}